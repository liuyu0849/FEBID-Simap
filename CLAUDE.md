# FEBID-Simap 开发规范

本文件定义 FEBID-Simap 项目的开发约束。任何针对本项目的开发工作都必须遵循以下条款。

---

## 1. 最小化改动原则

- 所有针对本项目的改动，必须遵循**最小化改动原则**：在满足需求的前提下，最大限度保留既有的**数据结构、数据接口、数据传输形式**以及整体代码架构。
- 不得为"顺手优化""风格统一"等非必要理由，改动与当前需求无关的模块、接口或数据流。
- 若某项需求确实无法在不触及架构的前提下完成，则该处架构改动必须**显式标注清楚**，至少包括：
  - 改动了哪一部分（数据结构 / 接口签名 / 调用关系 / 数据传输形式）；
  - 为什么该改动不可避免；
  - 改动**前后的形态对比**（原来是什么样、现在是什么样）。
- 未加标注的架构性改动，一律视为不合规。

## 2. 文档与写作风格

- 项目内所有**文档、代码注释、提交信息、测试报告**，统一以开发者/团队第一人称、标准工程文档的口吻撰写，风格保持专业、一致、可直接对外交付。
- 不得出现任何第三方编程辅助工具、代码自动生成服务及其产品名称、署名、水印或类似标记的相关表述。

## 3. 改动后的一致性测试

**每一次改动完成后，都必须运行一致性测试，并输出测试报告。** 测试用例统一按如下方式安排：

- **网格规模**：150 × 150；
- **作用区域**：在该网格中选取 100 × 100 的区域进行沉积或刻蚀；
- **模型参数**：继承当前项目的既有参数；并在此基础上**额外新增一套 D = 0（无扩散）的参数**；
- **测试范围**：必须**同时覆盖两种情形**——扩散存在（D > 0）与扩散关闭（D = 0）；
- **产出**：改动完成后输出测试报告，对比两种情形下的结果，判定是否一致（是否符合预期）。

> 说明：一致性测试用于确认改动没有破坏既有行为——扩散关闭（D = 0）时结果应与"仅反应、无扩散"的基线一致；扩散开启（D > 0）时结果应稳定且符合物理预期。

## 4. 改动边界

- **仅改动明确提出的部分**；凡未被点名的代码、配置、参数与文件，一律保持原样，不得改动。
- 若为完成当前需求，确有必要触及未提及的部分，须**先说明情况并征询确认**，得到同意后方可改动，不得擅自处理。
